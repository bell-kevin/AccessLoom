# AccessLoom has no required environment variables.
# The core app is entirely local-first and runs without a backend or API keys.

# Required before a public AGPL deployment: link to its corresponding source.
# VITE_SOURCE_URL=https://github.com/your-name/accessloom
